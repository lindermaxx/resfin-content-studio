# Story 1.2 — Deploy Vercel + Vercel KV + variáveis de ambiente

> **Status:** Done
> **Epic:** EPIC-01
> **Criado por:** @sm (River)
> **Data:** 2026-02-22
> **Complexidade:** S (pequena — configuração de infra)

---

## Story

**Como** Max Linder,
**Quero** que o app esteja acessível via URL pública no Vercel com o Vercel KV conectado e todas as variáveis de ambiente configuradas,
**Para que** eu possa acessar o ResFin Content Studio pelo navegador em qualquer lugar, sem precisar rodar o projeto localmente.

---

## Acceptance Criteria

- [x] AC1: Projeto importado no Vercel conectado ao repositório GitHub `lindermaxx/resfin-content-studio`
- [x] AC2: Deploy automático ativo — push na branch `main` dispara novo deploy
- [x] AC3: App abre sem erros em https://resfin-content-studio.vercel.app com os 5 módulos visíveis
- [x] AC4: Supabase provisionado em supabase.com (substituiu Vercel KV — não disponível no free tier Vercel)
- [x] AC5: Variáveis de ambiente configuradas no Vercel: `ANTHROPIC_API_KEY`, `GOOGLE_AI_API_KEY`, `GOOGLE_IMAGE_MODEL`, `GOOGLE_TEXT_MODEL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
- [x] AC6: Pacote `@supabase/supabase-js` instalado e cliente criado em `src/lib/supabase.ts`
- [x] AC7: `npm run build` passa sem erros

---

## Scope

**IN:**
- Importar repositório GitHub no Vercel e fazer primeiro deploy
- Provisionar Vercel KV e vincular ao projeto
- Configurar variáveis de ambiente no Vercel Dashboard
- Instalar `@vercel/kv` e criar `src/lib/kv.ts` com cliente pronto para uso

**OUT:**
- Nenhum uso real do KV nesta story (apenas setup — uso começa no EPIC-04)
- Nenhum domínio customizado (usar URL padrão Vercel)
- Nenhuma configuração de CI/CD além do deploy automático padrão do Vercel

---

## Tasks

### Tarefas do @dev (código)
- [x] 1. Instalar `@vercel/kv`: `npm install @vercel/kv`
- [x] 2. Criar `src/lib/kv.ts` com cliente KV exportado
- [x] 3. Atualizar `.env.local.example` com as variáveis do Vercel KV
- [x] 4. Verificar: `npm run build` passa sem erros
- [x] 5. Commit e push das alterações para `main`

### Tarefas manuais do Max (Vercel Dashboard)
- [x] 6. Acessar [vercel.com](https://vercel.com) e fazer login (ou criar conta)
- [x] 7. Clicar em "Add New Project" → importar `lindermaxx/resfin-content-studio`
- [x] 8. Deploy automático — aguardar build passar na Vercel
- [x] 9. Supabase provisionado em supabase.com (substituiu Vercel KV)
- [x] 10. Em Settings → Environment Variables: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, GOOGLE_AI_API_KEY, GOOGLE_IMAGE_MODEL, GOOGLE_TEXT_MODEL, ANTHROPIC_API_KEY
- [x] 11. Redeploy realizado — app acessível em https://resfin-content-studio.vercel.app

---

## Dev Notes

- O cliente KV em `src/lib/kv.ts` deve importar de `@vercel/kv` e exportar a instância pronta
- Em ambiente local (sem KV), o cliente pode retornar `null` graciosamente — o KV só é exigido em produção
- As variáveis `KV_URL`, `KV_REST_API_URL`, `KV_REST_API_TOKEN` e `KV_REST_API_READ_ONLY_TOKEN` são geradas automaticamente pelo Vercel quando o KV é conectado
- `GOOGLE_IMAGE_MODEL=gemini-3-pro-image-preview` e `GOOGLE_TEXT_MODEL=gemini-2.0-flash` já estão no `.env.local.example`
- Não expor nenhuma API key no lado cliente — todas as vars sem prefixo `NEXT_PUBLIC_` ficam apenas no servidor

---

## Definition of Done

- [ ] `npm run build` passa sem erros ou warnings
- [ ] App abre na URL pública Vercel sem erros
- [ ] Vercel KV aparece como "Connected" no dashboard do projeto
- [ ] Todas as variáveis de ambiente visíveis em Settings → Environment Variables do projeto Vercel
- [ ] `src/lib/kv.ts` presente e `@vercel/kv` no `package.json`

---

## Dependencies

**Depende de:** Story 1.1 (repositório GitHub com código base) ✅ Done

---

## Change Log

| Data | Autor | Ação |
|---|---|---|
| 2026-02-22 | @sm (River) | Story criada |
